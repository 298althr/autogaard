{
  "name": "Autogaard",
  "version": "1.0.0",
  "description": "Nigeria's AI-powered used car marketplace with real-time auctions",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\" --names \"server,client\" --prefix-colors \"blue,green\"",
    "migrate": "node server/db/migrate.js",
    "seed": "node server/db/seeds/catalog_seed.js && node server/db/seeds/demo_seed.js",
    "seed:catalog": "node server/db/seeds/catalog_seed.js",
    "seed:demo": "node server/db/seeds/demo_seed.js",
    "test": "npm run test --prefix server && npm run test --prefix client",
    "test:server": "npm run test --prefix server",
    "test:client": "npm run test --prefix client",
    "build": "npm run build --prefix client",
    "start": "node server/index.js",
    "lint": "npm run lint --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
